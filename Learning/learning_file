🧠 How to know which account is connected

git config --global user.name
git config --global user.email








✅ First Time Uploading to GitHub

git init                                # Initialize Git in the project folder
git add .                               # Stage all files
git commit -m "Initial commit"          # Commit the staged files
git remote add origin https://github.com/your-username/repo-name.git  # Link to GitHub
git push -u origin master               # Push and set upstream

🔁 After Uploading — On Any Future Changes

git status                              # Check for modified/untracked files
git add .                               # Stage your changes
git commit -m "Your commit message"     # Commit the changes
git push                                # Push to GitHub










